name: Test retries=0 setting
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          method: "chat.postMessage"
          token: "xoxb-invalid-token-for-testing"
          retries: "0"
          payload: |
            {"channel": "C0000000000", "text": "Hello from retries test"}
      - name: Assert action ran with retries=0 and produced outputs
        env:
          ACTUAL_OK: ${{ steps.action.outputs.ok }}
          ACTUAL_TIME: ${{ steps.action.outputs.time }}
        run: |
          echo "ok: $ACTUAL_OK"
          echo "time: $ACTUAL_TIME"
          if [ "$ACTUAL_OK" = "true" ]; then
            echo "FAIL: expected ok=false for invalid token with retries=0"
            exit 1
          fi
          echo "PASS: ok is not true as expected"
          if [ -z "$ACTUAL_TIME" ]; then
            echo "FAIL: expected time output to be set"
            exit 1
          fi
          echo "PASS: time output is set: $ACTUAL_TIME"
